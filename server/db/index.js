const cacheDb = require('./cache.db');
const userDb = require('./user.db')
const imageDb = require('./image.db');
const collectionDb = require('./collection.db')
const statisticsDb = require('./statistics.db');

module.exports = {
    cacheDb,
    userDb,
    imageDb,
    collectionDb,
    statisticsDb
}